#pragma once

// enum para los cambios de estado
enum class State {
    Menu,
    DifficultySelection,
    Playing,
    Pause,
    Options,
    GameOver,
    TravelConfirmation,
    InPlanet,
    ShipMenu
};